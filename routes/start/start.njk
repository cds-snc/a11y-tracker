{% extends "base.njk" %}
{% from 'banner.njk' import banner as banner %}

{% block content %}
    <h1>{{ __('start.title') }}</h1>

    {{ banner('blue', '<p>' + __('start.intro.banner') + '</p>') }}

{% endblock %}
